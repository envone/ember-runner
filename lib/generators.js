var runner = require('./runner');

runner.task('gen:app', 'Generate application', function(callback) {
  
});

runner.task('gen:ember-config', 'Generate ember_runner_config file', function(callback) {
  
});

runner.task('gen:main.js', 'Generate main.js', function(callback) {
  
});

runner.task('gen:default.less', 'Generate default.less', function(callback) {
  
});

runner.task('gen:sample.handlebars', 'Generate sample.handlebars', function(callback) {
  
});

runner.task('gen:sample.handlebars', 'Generate sample.handlebars', function(callback) {
  
});